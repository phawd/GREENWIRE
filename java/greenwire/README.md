GREENWIRE Java module (skeleton)

This module is a placeholder Java entrypoint which mirrors the Python CLI at a very high level.
Build with gradle:

    cd java/greenwire
    gradle build

Run:

    gradle run --args='--help'
